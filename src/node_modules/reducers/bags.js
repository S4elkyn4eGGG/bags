const initialState = {};

const bags = (state = initialState, { type, payload }) => {
    switch (type) {
        case 'ADD_BAG':
            return state;
        default:
            return state;
    }
};

export default bags;